<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Main content -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/background">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:elevation="6dp">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/primary"
                app:title="@string/app_name"
                app:titleTextColor="@color/white"
                app:titleTextAppearance="@style/TextAppearance.MaterialComponents.Headline6"
                app:navigationIcon="@drawable/ic_menu"
                app:navigationIconTint="@color/white" />

        </com.google.android.material.appbar.AppBarLayout>

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_behavior="@string/appbar_scrolling_view_behavior">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvRoutines"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:paddingStart="18dp"
                android:paddingEnd="18dp"
                android:paddingTop="18dp"
                android:paddingBottom="150dp"
                tools:listitem="@layout/item_routine" />

            <!-- Empty state -->
            <LinearLayout
                android:id="@+id/tvEmptyHint"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:orientation="vertical"
                android:gravity="center"
                android:paddingHorizontal="24dp"
                android:visibility="gone">

                <FrameLayout
                    android:layout_width="88dp"
                    android:layout_height="88dp"
                    android:background="@drawable/bg_badge_slate"
                    android:layout_marginBottom="20dp">

                    <ImageView
                        android:layout_width="42dp"
                        android:layout_height="42dp"
                        android:layout_gravity="center"
                        android:src="@drawable/ic_clock"
                        app:tint="@color/hint"
                        android:contentDescription="@null" />

                </FrameLayout>

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="아직 루틴이 없어요"
                    android:textSize="18sp"
                    android:fontFamily="@font/pretendard_bold"
                    android:textColor="@color/text_secondary"
                    android:layout_marginBottom="8dp" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:lineSpacingExtra="4dp"
                    android:text="우측 하단 + 버튼을 눌러\n첫 연습 루틴을 추가해 보세요."
                    android:textSize="14sp"
                    android:fontFamily="@font/pretendard_medium"
                    android:textColor="@color/hint" />

            </LinearLayout>

            <!-- Bottom gradient + start button -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:orientation="vertical"
                android:background="@drawable/bg_bottom_fade"
                android:paddingHorizontal="18dp"
                android:paddingTop="24dp"
                android:paddingBottom="18dp">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnStart"
                    android:layout_width="match_parent"
                    android:layout_height="60dp"
                    android:text="연습 시작"
                    android:textSize="18sp"
                    android:fontFamily="@font/pretendard_extrabold"
                    android:textColor="@color/white"
                    android:backgroundTint="@color/primary"
                    app:cornerRadius="18dp"
                    app:icon="@drawable/ic_play_filled"
                    app:iconGravity="textStart"
                    app:iconTint="@color/white"
                    app:iconSize="22dp" />

            </LinearLayout>

        </FrameLayout>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabAdd"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="22dp"
            android:layout_marginBottom="92dp"
            android:contentDescription="루틴 추가"
            android:src="@drawable/ic_plus"
            app:tint="@color/white"
            app:backgroundTint="@color/accent"
            app:borderWidth="0dp"
            app:elevation="8dp"
            app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.Fab.Squircle" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- Navigation Drawer -->
    <LinearLayout
        android:id="@+id/navDrawer"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:orientation="vertical"
        android:background="@color/card_background"
        android:elevation="8dp">

        <!-- Gradient header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_drawer_header"
            android:paddingStart="22dp"
            android:paddingEnd="22dp"
            android:paddingTop="28dp"
            android:paddingBottom="22dp">

            <FrameLayout
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="@drawable/bg_icon_badge_white"
                android:layout_marginBottom="14dp">

                <ImageView
                    android:layout_width="26dp"
                    android:layout_height="26dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_clock"
                    app:tint="@color/white"
                    android:contentDescription="@null" />

            </FrameLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/app_name"
                android:textColor="@color/white"
                android:textSize="20sp"
                android:fontFamily="@font/pretendard_extrabold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="v1.2"
                android:textColor="@color/white_alpha"
                android:textSize="12sp"
                android:layout_marginTop="3dp" />

        </LinearLayout>

        <!-- Drawer items -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <LinearLayout
                android:id="@+id/drawerItemSelect"
                style="@style/DrawerRow">

                <FrameLayout style="@style/DrawerIconWrap" android:background="@drawable/bg_badge_blue">
                    <ImageView style="@style/DrawerIcon" android:src="@drawable/ic_check_square" app:tint="@color/primary" />
                </FrameLayout>

                <TextView style="@style/DrawerLabel" android:text="선택 삭제" />

            </LinearLayout>

            <LinearLayout
                android:id="@+id/drawerItemSave"
                style="@style/DrawerRow">

                <FrameLayout style="@style/DrawerIconWrap" android:background="@drawable/bg_badge_purple">
                    <ImageView style="@style/DrawerIcon" android:src="@drawable/ic_save" app:tint="@color/secondary" />
                </FrameLayout>

                <TextView style="@style/DrawerLabel" android:text="저장하기" />

            </LinearLayout>

            <LinearLayout
                android:id="@+id/drawerItemLoad"
                style="@style/DrawerRow">

                <FrameLayout style="@style/DrawerIconWrap" android:background="@drawable/bg_badge_green">
                    <ImageView style="@style/DrawerIcon" android:src="@drawable/ic_folder" app:tint="@color/accent" />
                </FrameLayout>

                <TextView style="@style/DrawerLabel" android:text="불러오기" />

            </LinearLayout>

            <LinearLayout
                android:id="@+id/drawerItemSettings"
                style="@style/DrawerRow">

                <FrameLayout style="@style/DrawerIconWrap" android:background="@drawable/bg_badge_slate">
                    <ImageView style="@style/DrawerIcon" android:src="@drawable/ic_settings" app:tint="@color/text_secondary" />
                </FrameLayout>

                <TextView style="@style/DrawerLabel" android:text="설정" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>
